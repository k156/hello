import time
import mysys

def add_commit_push():
    cd C:\workspace\hello
    git add --all
    git commit -am "{}-{} 강의".format(msg)
    if msg == false:
        msg = (datetime.datetime.now.month, datetime.datetime.now.day)
    git push -u origin master