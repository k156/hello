order = input("매번 초기화를 원하면 "가", 식을 계속 쌓아가고 싶으면 "나"를 입력")
if order = "가":
    cal()

    
class Cal:
    def __init__():
        strip().split()

    def plus():
        return a + b
        r = plus(a,b)
        print("정답은", r)

    def minus():
        return a - b
        e = minus(a,b)
        print("정답은", e)
    
    def multiply():
        return a * b
        w = multiply(a,b)
        print("정답은", w)

    def divide():
            if b == 0:
                return a
            return a / b
            q = divide(a,b)
            print("정답은{:.2f}", format(q))

    a = int(cals[0])
    b = int(cals[2])
    op = cals[1]


cal1 = Cal()

while(input == 0):
    cal = input("식을 입력하시오. ex)a + b ")

    if cal == "exit":
        break
    

    if op == "+":

    if op == "-":
    if op == "*":
    if op == "/":
        
       