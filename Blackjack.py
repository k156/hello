class Casting:
    def to_int(s):
        if type(s) == str:
            return int(s.strip())
        else:
            return s

class Deck:
    deck = [heart1,2,3,4,spade1,2,3,4,diamond1,2,3,4,clover1,2,3,4]
    def randomcard ():
        card = random.choic(deck)


class Game:

      while (<21):
        def start_game():
            for i in (0,1)
                self.card = deck에서 랜덤한 element를 고름.
            
            for number, card in enumerate():
                card lists = (card,number)
                
            Casting.to_int(카드이름에서 문자를 뺀 것)


class Player(Game):
    
    def __init__(self, name, money):
    self.name = input("{}번째 플레이어 이름을 입력하세요.")
    self.money = input(self.name"님, 배팅하고 싶은 금액을 입력하세요.")

  

        self.choice = input(self.name"님 차례입니다. Hit 하고싶으면 1, Stand 하고싶으면 2를 입력하세요.")
        if self.choice = 1:

    


class Dealer:
    card1=
    card2=

    def reveal():
        print(card1)


===================main flow=========================

dealer = Dealer()

num_of_players = input("Player 수를 입력하시오.")
Players = Casting.to_int(num_of_players)

if Players == 1:
    player1=Player()
elif Players == 2:
    player1=Player()
    player2=Player()
# iteration 통해서 정리하기.

